

services:
  frontend:
    build:
      context: ./UI
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=production
      - BACKEND_URL=http://your-backend-url.com  # Add your backend URL here